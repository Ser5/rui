export const baseColors: Record<string, string> = {
	red:   'Красный',
	green: 'Зелёный',
	blue:  'Синий',
}

export const baseColorEntries = Object.entries(baseColors);

export const colorCodesList = Object.keys(baseColors);
